# 天黑之前 · 环境变量示例
# ────────────────────────────────────────────────────────────────
# 复制本文件为 .env 并填入真值即可启用对应功能。
# .env 文件已被 .gitignore 排除（绝对不入仓）。
# ────────────────────────────────────────────────────────────────

# Anthropic Claude API 密钥（可选）
# ────────────────────────────────────────────────────────────────
# 不配：工具链走 Cline 协作模式（Cline 自己读 prompt 文件、自己写正文）
# 配  ：可解锁 run_dispatch.py 的远端派发能力（Claude API 写正文、Cline 审核）
#
# 获取：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# 自定义 API 网关（可选，默认直连 Anthropic 官方）
# 仅当你用中转服务（如自建网关 / 第三方代理）时需要填写
# ANTHROPIC_BASE_URL=

# Python 路径（Windows 开发用）
# ────────────────────────────────────────────────────────────────
# 如果 `python` 命令在你 PowerShell / IDE shell 解析不到真 Python
# （典型症状：exit code 9009 + stdout 空），可以填这里然后用：
#   python "D:/天黑之前/Novel_New/tools/run_with_env.py" engine/writer.py brief 14
#
# 默认：D:/Python/python.exe （天黑之前本机）
PYTHON_EXE=D:/Python/python.exe